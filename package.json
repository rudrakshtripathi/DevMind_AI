{
  "name": "devmind-ai",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "dotenv -- concurrently -n fe,be -c cyan,green \"pnpm --filter @workspace/devmind dev\" \"pnpm --filter @workspace/api-server dev\"",
    "build": "pnpm run build:frontend && pnpm run build:backend",
    "build:frontend": "pnpm --filter @workspace/devmind build",
    "build:backend": "pnpm --filter @workspace/api-server build",
    "start": "dotenv -- pnpm --filter @workspace/api-server start",
    "db:push": "dotenv -- pnpm --filter @workspace/db push",
    "db:push:force": "dotenv -- pnpm --filter @workspace/db push-force",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "vercel-build": "pnpm run build"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
